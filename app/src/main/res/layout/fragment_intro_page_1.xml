<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:icon="@drawable/symbol"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusableInTouchMode="true"
    android:id="@+id/fragment1"
    android:animateLayoutChanges="true">


        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/dunk_icon"
            android:id="@+id/anonymous_icon"
            android:layout_marginBottom="64dp"
            android:layout_above="@+id/textView2"
            android:layout_alignEnd="@+id/textView2" />


        <ImageView
            android:layout_width="150dp"
            android:layout_height="50dp"
            android:src="@drawable/intro_share_icons"
            android:id="@+id/intro_share_icon"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="146dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Record your"
        android:id="@+id/textView2"
        android:layout_above="@+id/intro_share_icon"
        android:layout_centerHorizontal="true" />

</RelativeLayout>